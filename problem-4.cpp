// maximum and minimum in devide and conquier method 

#include <bits/stdc++.h>
using namespace std;
int findMaxMin(vector<int>,int first,int last){
    
}
int main(){
    vector<int> vec;
    int n, element;
    cout << "Enter the size of the arry : ";
    cin >> n;
    for (size_t i = 0; i < n; i++)
    {
        cin >> element;
        vec.push_back(element);
    }
    return 0;
}